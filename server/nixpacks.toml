# nixpacks.toml generated for Node.js deployment

[phases.setup]
nixPkgs = ["nodejs", "npm"]

[phases.install]
cmds = ["npm install --production"]

[phases.build]
cmds = ["echo 'No build required'"]

[start]
cmd = "node printful-server.js"
